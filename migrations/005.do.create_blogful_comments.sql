CREATE TABLE comments (
  id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  commented_at TIMESTAMP DEFAULT now() NOT NULL,
  text TEXT NOT NULL,
  article_id INT REFERENCES blogful_articles(id) ON DELETE CASCADE NOT NULL,
  user_id INT REFERENCES blogful_users(id) ON DELETE CASCADE NOT NULL

)
